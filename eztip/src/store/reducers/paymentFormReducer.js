// import { } from '../types';

const initialState = {

};

export const paymentFormReducer = (state = initialState, action) => {
    switch(action.type) {
        default:
        return state;
    }
}